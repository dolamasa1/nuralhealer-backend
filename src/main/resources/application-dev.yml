spring:
  datasource:
    url: ${LOCAL_DB_URL}
    username: ${LOCAL_DB_USERNAME}
    password: ${LOCAL_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update # Automatic updates for dev convenience
    show-sql: true

logging:
  level:
    "[com.neuralhealer.backend]": DEBUG
    "[org.hibernate.SQL]": DEBUG
